module.exports = [
    null, // Just so the selections on the interactive menu will start from 1. slice this array from 1
    require('./fibonacci'),
    require('./clone'),
    require('./montyHall')
];